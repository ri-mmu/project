server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/softproj_cc?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: a3410300
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: 2012ssuclear@gmail.com
    password: uowwayeavwbmvfav
    protocol: smtp
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
    default-encoding: UTF-8

logging:
  level:
    root: info